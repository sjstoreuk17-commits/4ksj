{
  "entities": {
    "LicenseKey": {
      "title": "License Key",
      "description": "A secure access token for the platform.",
      "type": "object",
      "properties": {
        "key": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["key", "createdAt"]
    },
    "UserTracker": {
      "title": "User Tracker State",
      "description": "Stores the tracking activation date and notification preferences for a user.",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "trackerActive": { "type": "boolean" },
        "activatedAt": { "type": "string", "format": "date-time" },
        "lastCheckedAt": { "type": "string", "format": "date-time" },
        "xtreamSubDomain": { "type": "string" },
        "xtreamUsername": { "type": "string" }
      },
      "required": ["userId", "trackerActive"]
    }
  },
  "firestore": {
    "/trackers/{userId}": {
      "schema": "UserTracker",
      "description": "Individual user tracking states indexed by their unique device/session ID."
    },
    "/license_keys/{keyId}": {
      "schema": "LicenseKey",
      "description": "Unique terminal access keys generated by administrators."
    }
  }
}
